export type sourceFormat = {
    date: string;
    name: string;
    cost: number;
    currency: string;
    id: string;
};

export type destinationFormat = {
    date: string;
    text: string;
    category: string;
    cost: number;
};
